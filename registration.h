#ifndef REGISTRATION_H
#define REGISTRATION_H
#include <user.h>

class Registration
{
public:
    Registration();
    ~Registration();

public:
    //void newUser(int id, std::string log, std::string pass, std::string ip, unsigned short port);
};

#endif // REGISTRATION_H
